using System.Collections.Generic;
using UnityEngine;

public class TemplateCapacityReference : MonoBehaviour
{
    public string capacity_template_id = ""; // the id of the capacity template that this component references (if any)
    public List<string> capacity_template_ids = new List<string>(); // even more    
}